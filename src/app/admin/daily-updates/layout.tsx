import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin - Daily Updates",
};

export default function AdminDailyUpdatesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
