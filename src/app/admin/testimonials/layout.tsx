import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin - Manage Testimonials",
};

export default function AdminTestimonialsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
